/*import { MockBackend, MockConnection } from '@angular/http/testing';
import { Http, ConnectionBackend, BaseRequestOptions, Response, ResponseOptions } from '@angular/http';
import { discardPeriodicTasks, tick, async, inject, TestBed, fakeAsync } from '@angular/core/testing';
import { HttpModule, JsonpModule } from '@angular/http';

import { UnitMeasurementService } from './unit-measurement.service';

describe('UnitMeasurementService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [UnitMeasurementService]
    });
  });

  it('should be created', inject([UnitMeasurementService], (service: UnitMeasurementService) => {
    expect(service).toBeTruthy();
  }));

  it('should have a method to get all Unused Receipts',
    fakeAsync(inject([UnitMeasurementService, MockBackend], (unitMeasurementService: UnitMeasurementService, mockBackend: MockBackend) => {
      tick(2000);
    }))
  );
});
*/