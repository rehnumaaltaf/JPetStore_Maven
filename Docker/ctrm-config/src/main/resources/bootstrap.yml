spring:
  application:
    name: ctrm-config
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared
  profiles:
     active: native

server:
  port: 8888
  
security:
  user:
    password: bda5245f-10fc-4dc1-8e88-bbea687de3fb